# Demote background work (including uwsm's nested background-graphical.slice,
# i.e. apps launched with `uwsm app -s b`) to a quarter of app.slice's CPU
# share under contention.
[Slice]
CPUWeight=25
