# Keep background work (incl. apps launched with `uwsm app -s b`) off the
# reserved UI thread (CPU 7). Companion to 50-cpuweight.conf in this dir.
# See app.slice.d/50-cpuset.conf and README "UI CPU priority".
[Slice]
AllowedCPUs=0-6
