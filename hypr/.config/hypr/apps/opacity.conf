# Override Omarchy defaults in windows.conf: opacity 0.97 0.9 (unfocused was very transparent).
# Terminals use tag terminal + separate opacity in apps/terminals.conf — override those too.
windowrule = opacity 1.0 0.97, match:tag default-opacity
windowrule = opacity 1.0 0.97, match:tag terminal
