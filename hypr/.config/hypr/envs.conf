# Personal Hyprland environment variables (sourced from hyprland.conf).
# Kept out of monitors.conf so display tools (nwg-displays) that regenerate
# monitors.conf can't clobber it.

# Keep GTK apps at scale 1 on this mixed-DPI multi-monitor setup.
env = GDK_SCALE,1
